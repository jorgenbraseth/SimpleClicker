using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ground : Tile {

	//void OnMouseDown() {


//		float yOffset = 0f;
//
//		Buildable toBuild = buildable [0];
//		if(Input.GetKey(KeyCode.LeftShift)) {
//			toBuild = buildable [1];
//			yOffset = 0.2f;
//		}
//
//		
//
//		if (GameState.instance.cash < toBuild.cost) {
//			GameState.instance.soundManager.PlayError ();
//			return;
//		}
//
//		GameState.instance.cash -= toBuild.cost;
//
//		Tile built = Instantiate (toBuild, new Vector3(
//			transform.position.x, 
//			transform.position.y + yOffset, 
//			transform.position.z), 
//			transform.rotation);
//
//		ReplaceWith (built);
//		GameState.instance.soundManager.PlaySound (toBuild.buildSound);

//	}

}
